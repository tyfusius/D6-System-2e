import type { Migration } from "@d6-system-2e/core";

export const addSecondEditionCombatStateMigration: Migration = Object.freeze({
  name: "Admit the persistent Second Edition condition track",
  version: 6,
});
